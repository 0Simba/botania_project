package init;

import engine.popUpEngine.PopUpEngineMain;
import utils.Vector2;

class PopUp
{

    static public function load () {
        var popUp = PopUpEngineMain.getInstance();
        //popUp.create("test", new Vector2(0.8, 0.8), new Vector2(0.1, 0.1), "ground", "savana");
    }

}